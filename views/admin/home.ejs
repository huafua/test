<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>管理员首页</title>
    <link rel="stylesheet" href="/static/public/admin/css/home.css">
</head>
<body>
        <div class="header">
            <a href="/" class="inf left"><img src="/static/public/common/images/logo.png" alt=""/></a>
            <a  class="inf" href="/admin/logout">注销</a>
            <span class="inf"><%=user.username.toUpperCase()%></span>
        </div>
        <div class="content-container">
            <div class="search">
                <form action="/admin/search" method="get">
                    <input type="text" name="keyword" value="<%=keyword%>" placeholder="请输入关键字儿"/>
                    <input type="submit" value="查找">
                </form>
            </div>
           <div class="content">
               <table>
                   <caption>人员清单</caption>
                   <thead>
                       <tr>
                            <th>头像</th>
                            <th>姓名</th>
                            <th>性别</th>
                            <th>描述</th> 
                            <th><a class="btn btn-add" href="/admin/add">添加</a></th>
                        </tr>
                   </thead>
                   <tbody>
                    <% for(var i=0;i<data.length;i++){%>
                        <tr>
                            <td><img class="person-image" src="/static/<%=data[i].img%>" alt=""></td>
                            <td><%=data[i].username%></td>
                            <td><%=data[i].gender%></td>
                            <td style="text-align: left"><%=data[i].desc%></td>
                            <td>
                                <a class="btn btn-remove" href="/admin/remove/<%=data[i].id%>" onclick="return confirm('确定删除?')">删除</a>
                                <a class="btn btn-edit" href="/admin/update/<%=data[i].id%>">编辑</a>
                            </td>
                        </tr>
                    <%}%>
                   </tbody>
               </table>
           </div>
        </div>
        <div class="footer"></div>
</body>
</html>